export enum Genders {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
}

export enum Roles {
  CUSTOMER = 'CUSTOMER',
  ARCHITECT = 'ARCHITECT',
}

export enum OrderStatuses {
  ACCEPTED = 'ACCEPTED',
  REFUSED = 'REFUSED',
  OPENED = 'OPENED',
  DELETED = 'DELETED',
}
